{
    "tasks": [
        {
            // "type": "cppbuild",
            "type": "shell",
            "label": "C/C++: gcc build active file",
            // "command" : "gcc -fdiagnostics-color=always -g ${workspaceFolder}/*.c -o ${workspaceFolder}/d.out",
            "command": "/usr/bin/gcc",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                // "${file}",
                "${workspaceFolder}/*.c",
                "-o",
                // "${fileDirname}/${fileBasenameNoExtension}",
                "${workspaceFolder}/d.out",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}